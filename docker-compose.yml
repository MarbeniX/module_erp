version: "3.8"

services:
    postgres:
        image: postgres:16
        container_name: module_erp_postgres
        restart: unless-stopped
        environment:
            POSTGRES_USER: berlinol
            POSTGRES_PASSWORD: "Gabriela14+"
            POSTGRES_DB: mydb
        ports:
            - "5432:5432"
        volumes:
            - postgres_data:/var/lib/postgresql/data

volumes:
    postgres_data:
